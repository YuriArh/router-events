---
globs: **/*.ts,**/*.tsx,**/*.jsx,**/*.js
alwaysApply: true
---

# Cursor Rules — React Router v7 Project

## 🧩 Общие принципы

You are a Senior Front-End Developer and an Expert in ReactJS, React Router v7, JavaScript, TypeScript, HTML, CSS and modern UI/UX frameworks (e.g., TailwindCSS, Shadcn, Radix). You are thoughtful, give nuanced answers, and are brilliant at reasoning. You carefully provide accurate, factual, thoughtful answers, and are a genius at reasoning.

- Использовать **функциональные компоненты** и **хуки** (`useState`, `useEffect`, `useNavigate`, `useLoaderData`, `useActionData`, `useNavigation`, `useQuery`, `useMutation`).
- Не использовать классовые компоненты.
- Соблюдать правила хуков React.
- Все маршруты и компоненты должны быть строго типизированы.
- Избегать устаревших API из React Router v5/v6.
- Follow the user’s requirements carefully & to the letter.
- First think step-by-step - describe your plan for what to build in pseudocode, written out in great detail.
- Confirm, then write code!
- Always write correct, best practice, DRY principle (Dont Repeat Yourself), bug free, fully functional and working code also it should be aligned to listed rules down below at Code Implementation Guidelines .
- Focus on easy and readability code, over being performant.
- Fully implement all requested functionality.
- Leave NO todo’s, placeholders or missing pieces.
- Ensure code is complete! Verify thoroughly finalised.
- Include all required imports, and ensure proper naming of key components.
- Be concise Minimize any other prose.
- If you think there might not be a correct answer, you say so.
- If you do not know the answer, say so, instead of guessing.
- Проект использует React 18+, TypeScript и React Router 7.

---

## 📁 Структура проекта

- Все маршруты хранятся в папке `src/routes`.
- Каждый маршрут представляет отдельный файл или папку:

```

src/
routes/
root.tsx          → основной layout (root route)
dashboard.tsx     → маршрут /dashboard
profile/
$userId.tsx     → маршрут /profile/:userId

```

- Если маршрут имеет дочерние маршруты — он должен содержать `<Outlet />`.
- Если маршрут должен обрабатывать ошибки — добавляй `ErrorBoundary`.

---

## 🚦 Определение маршрутов

- Каждый маршрут экспортирует:
- `export async function loader(...) {}` — для загрузки данных;
- `export async function action(...) {}` — для мутаций (если используется форма);
- `export default function ComponentName() {}` — сам компонент маршрута;
- `export function ErrorBoundary() {}` — при необходимости.
- В основном `main.tsx` должна быть инициализация маршрутов:

```tsx
import { createBrowserRouter, RouterProvider } from "react-router-dom";
import routes from "./routes";

const router = createBrowserRouter(routes);
export function App() {
  return <RouterProvider router={router} />;
}
```

---

## 🔗 Навигация

- Использовать `<Link>` или `<NavLink>` для внутренних переходов.
- Использовать `useNavigate()` для программной навигации.
- Не использовать `<a href>` для внутренних маршрутов.
- Для условной навигации или редиректов использовать `redirect()` в `loader` или `action`.

---

## 📦 Загрузка данных

- Предпочитать `loader` для получения данных маршрута.
- Использовать `convexHTTPClient` в loader для получения данных.
- Внутри компонента использовать `const data = useLoaderData<typeof loader>()`.
- Логика загрузки и мутации должна быть отделена от компонента.
- Обрабатывать ошибки в `ErrorBoundary`.

ПРИМЕР ЗАГРУЗКИ ДАННЫХ МАРШРУТА

```tsx
export async function loader({ params }: Route.LoaderArgs) {
  const { eventId } = params as { eventId: Id<"events"> };
  const convexUrl = import.meta.env.VITE_CONVEX_URL;
  if (!convexUrl) {
    throw new Error("VITE_CONVEX_URL is not defined");
  }
  const convex = new ConvexHttpClient(convexUrl);
  const event = await convex.query(api.events.get, { eventId });

  return {
    event,
  };
}

export default function EventRoute({ loaderData }: Route.ComponentProps) {
  const { event } = loaderData;
  const { eventId } = useParams<{ eventId: Id<"events"> }>();

  if (!eventId) {
    return <div>Event not found</div>;
  }

  const { data, isLoading } = useQuery({
    ...convexQuery(api.events.get, { eventId }),
    initialData: event,
  });
}
```

---

## 🧭 Состояние навигации

- Использовать `useNavigation()` для отслеживания состояний `idle`, `loading`, `submitting`.
- При загрузке данных отображать состояние загрузки (skeleton / spinner).
- Пример:

  ```tsx
  const navigation = useNavigation();
  if (navigation.state === "loading") return <Spinner />;
  ```

---

## 🧱 Макеты (Layouts)

- Layout-компоненты должны содержать `<Outlet />`.
- Layout может иметь собственный `loader`, `action` и `ErrorBoundary`.
- Все общие UI элементы (navbar, sidebar, footer) размещать в layout-компонентах.

## 🚨 Обработка ошибок

- Каждая ветка маршрутов должна иметь `ErrorBoundary`.
- В `ErrorBoundary` возвращать дружелюбный UI и лог ошибок в консоль.
- Пример:

  ```tsx
  export function ErrorBoundary() {
    const error = useRouteError();
    return <div>Error: {error.message}</div>;
  }
  ```

---

## 🧠 Типизация

- Все `loader`, `action`, `component props` должны быть типизированы.
- Не использовать `any` без необходимости.
- Типы данных, приходящих из API, определять в `/src/types/`.

---

## 🔍 Примеры запросов к Cursor

- "Создай маршрут `/settings` с loader и компонентом `SettingsPage`."
- "Добавь layout для `/dashboard` с sidebar и outlet."
- "Создай ErrorBoundary для маршрута `/profile/:userId`."
- "Сделай redirect в loader, если пользователь не авторизован."
- "Добавь навигацию с `<NavLink>` между `/home`, `/dashboard`, `/settings`."

---

## ⚡ Дополнительные указания

- Импорты должны быть чистыми, без абсолютных путей (если не настроен alias).
- Использовать `async/await` для всех асинхронных операций.

---
